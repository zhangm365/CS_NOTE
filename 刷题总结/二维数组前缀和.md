
# 二维数组的前缀和

```c++

// 初始二维数组
vector<vector<int>> grid;

int m = grid.size(), n = grid[0].size();

// 二维数组：前缀和
vector<vector<int>> preSum(m+1, vector<int>(n+1, 0));


for(int i = 1; i <= m; ++i) {
    for(int j = 1; j <= n; ++j) {
        preSum[i][j] = preSum[i][j-1] + grid[i-1][j-1];
    }
}

for(int i = 1; i <= m; ++i) {
    for(int j = 1; j <= n; ++j) {
        preSum[i][j] = preSum[i-1][j] + grid[i-1][j-1];
    }
}


```