

# 判断一个字符串是否为回文串

```cpp

// 判断 str[i, j] 内的字符串是否为回文串 
bool isPalindrome(const string& str, int i, int j) {
    while(i < j && str[i] == str[j]) {
        i++;
        j--;
    }

    return i >= j;
}


```
